[project]
name = "api-payment"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi==0.141.1",
    "granian[uvloop]>=2.8.1",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ruff>=0.16.3",
    "ty>=0.0.73",
]
